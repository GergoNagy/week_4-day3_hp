<form method="post" action="/students">

  <!-- First name/ field -->
  <label for="first-name-field">First Name:</label>
  <input type="text" name="first_name" id="first-name-field"/>

  <!-- Last name/ field -->
  <label for="last-name-field">Last Name:</label>
  <input type="text" name="last_name" id="last-name-field"/>

  <!-- House/ drop-down -->
  <label for="house-selection">House:</label>
  <select name="house_id" id="house-selection">
    <% @houses.each do |house| %>
      <option value="<%=house.id%>"><%=house.name%></option>
    <% end %>  
  </select>

  <!-- Age/ field -->
  <label for="age-field">Age:</label>
  <input type="number" name="age" id="age-field"/>

  <!-- Submit -->
  <input type="submit" value="Submit Student"/>

</form>